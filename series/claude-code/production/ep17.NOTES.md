# EP 17 — production notes (`/init` → CLAUDE.md)

**Topic:** `/init` auto-generates a CLAUDE.md. Docs: https://code.claude.com/docs/en/memory
(*"Run `/init` to generate a starting CLAUDE.md automatically. Claude analyzes your codebase and creates a file
with build commands, test instructions, and project conventions it discovers."*)

**Angle:** demonstrated on a *docs/markdown* project (the move-abroad plan), reinforcing the series' "not just for
code" thread — shown as an **observation**, not asserted as a documented guarantee (the docs frame `/init` around a
codebase, so the captions deliberately avoid the words "works on any project").

## ⛔ SENSITIVE-DATA GATE — do NOT record against the real repo

The story repo `Stig-Johnny/gran-canaria-plan` is the operator's **real** family-relocation + Invotek income plan.
`/init` scans the **whole** project and streams its work to the terminal — it lists the directory (exposing sensitive
**filenames**) and reads files (README.md and `INVOTEK_BRAIN.md` alone leak DOB, org.nr, MVA, phone, business/forwarding
addresses, income status, named third parties, and an annotated index of every sensitive file). A recording of `/init`
on the real repo **publishes all of that**. There is no safe way to run the advertised command against the real repo.

### Record against the committed SYNTHETIC stand-in (do NOT clone the real repo)
**DECISION (hardened 2026-06-15): use the 100% synthetic fixture, never the real repo.** The earlier recipe —
"clone the real repo + allowlist a few files" — is itself unsafe: a sibling file can carry a hidden PII reference, and
a stale full clone can be left staged in the render path. (On 2026-06-15 a full real-repo clone was found staged at
`/tmp/gcp-ep17` — byte-identical to the real `Stig-Johnny/gran-canaria-plan`, full PII — and removed. Don't recreate
that pattern.) Instead, a **fictional, byte-for-byte synthetic** stand-in now lives in the repo, verified PII-clean by
an adversarial audit (zero names / DOB / org.nr / MVA / phone / address / income / third parties), and the simulated
`/init` → predicted CLAUDE.md was confirmed screen-safe:

```
series/claude-code/demo-assets/ep17-standin/
  README.md  kalender.html  research.html
  gran-canaria-prepurchase-checklist.md  obsidian-oppsett.md  styles.css
```

Render-time prep — copy the **committed fixture** (NOT the real repo) into a scratch dir and init it so `/init` has a
fresh repo with no CLAUDE.md yet:

```bash
# from a clone of run-the-docs/engine on the render host
SRC="$PWD/series/claude-code/demo-assets/ep17-standin"   # the COMMITTED synthetic fixture
DST=~/runthedocs/scratch/cc-ep17-demo
rm -rf "$DST"; mkdir -p "$DST"
cp "$SRC"/* "$DST"/
cd "$DST" && git init -q && git add -A && git commit -qm "synthetic demo project for ep17"
# THEN: open Claude Code here, run /init, and review the generated CLAUDE.md frame-by-frame before publishing.
```

> ⛔ **FORBIDDEN render sources — never point the recorder at any of these:** `Stig-Johnny/gran-canaria-plan` (the real
> repo), `~/repos/gran-canaria-plan` (operator's working clone), or any `/tmp/gcp-ep17*` / scratch clone of the real
> repo. The ONLY approved source is the committed synthetic fixture above. Even so, **review the generated CLAUDE.md
> frame-by-frame** and confirm no `ls` / file-tree / scan frame shows anything but the six synthetic filenames.

## Shot list (≈30s)
1. `cd` into the sanitized project — show the directory has **no CLAUDE.md** yet (the before-state; payoff = "file
   appears from nothing", which differentiates from EP 3).
2. Type `/init` — make the **command card the hero** (EP 3 already owns the CLAUDE.md *file* card; the fresh thing here
   is the command doing it for you).
3. Claude scans + writes `CLAUDE.md`; show the file-card appear (filename only) and only the top summary/structure
   section of the generated file (verified clean per the gate above).

## Render checklist
- Dual format: `ep17-linkedin-4x5.mp4` (1080×1350) + `ep17-vertical-9x16.mp4` (1080×1920).
- Accent `#da7756` on `#0a0a0a`, JetBrains Mono / Menlo.
- **Footer must read "Run the Docs"** (e.g. `Run the Docs - daily Claude Code tips`), NOT the old "Invotek" burn-in
  (the footer bug fixed in ep6–11 — don't reintroduce it).
- Audio 48 kHz stereo before any YouTube upload.
- The per-episode `cc-demo-repo` reset in `recd.sh` does **not** apply to ep17 — point the recorder at the sanitized
  stand-in instead, and confirm no `ls` / file-tree / scan frame in the final cut shows an excluded filename.

## Copy
Captions: `../posting-kit/ep17-copy.md` (LinkedIn / TikTok / YouTube / X — softened for docs-fidelity and
front-loaded with the outcome hook; verified against the live docs 2026-06-15, safe to post as-is).
