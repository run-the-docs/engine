# Ep 16 — Let Claude interview you (the hard questions first)

**Suggested slot:** weekday 09:00 CET
**Files:**
- `ep16-linkedin-4x5.mp4` (1080×1350 — LinkedIn + X)
- `ep16-vertical-9x16.mp4` (1080×1920 — TikTok / YouTube Shorts / Reels)

---

## LinkedIn (upload the 4:5 natively — no link in the body)

Claude built the wrong thing because you rushed the prompt?

For a big feature, flip it. Tell Claude to interview you using the AskUserQuestion tool, and dig into the decisions you didn't think about.

I asked it to design a rate limiter. Its first question wasn't the obvious stuff. It went straight for the one that bites you in production: when the backend state store goes down, do you fail open or fail closed?

Have it write the full spec to a file, then open a fresh session and build from that.

**First comment:**
> Watch it: [YouTube link]

#ClaudeCode #AICoding #DeveloperTools

---

## TikTok / Instagram Reels

claude built the wrong thing because you rushed the prompt? for a big feature, flip it. tell claude to interview you with the AskUserQuestion tool. i asked it to design a rate limiter and its first question went straight for the hard part: when the backend goes down, do you fail open or fail closed? then have it write the spec to a file and build from that. follow for a Claude Code tip a day.

#claudecode #aicoding #devtools #coding

---

## YouTube Shorts

**Title:** Make Claude interview YOU before it builds

**Description:**
For a big feature, have Claude interview you using the AskUserQuestion tool. It surfaces the edge cases and tradeoffs you would have skipped. Then have it write the full spec to a file and start a fresh session to build from it.

Run the Docs, Claude Code series. A project by Invotek.

#Shorts #ClaudeCode #AICoding

---

## X / Twitter

Claude built the wrong thing because you rushed the prompt?

For a big feature, flip it. Tell Claude to interview you with the AskUserQuestion tool. I asked it to design a rate limiter and its first question went straight for the hard tradeoff: fail open or fail closed when the backend is down? Then have it write the spec and build from a fresh session.
