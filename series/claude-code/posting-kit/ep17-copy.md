# Ep 17 — Let Claude write its own onboarding (`/init` → CLAUDE.md)

**Suggested slot:** weekday 09:00 CET
**Files:**
- `ep17-linkedin-4x5.mp4` (1080×1350 — LinkedIn + X)
- `ep17-vertical-9x16.mp4` (1080×1920 — TikTok / YouTube Shorts / Reels)

> ⚠️ **PRODUCTION SAFETY (read `../production/ep17.NOTES.md` before recording).** The story references a *real* private repo (`gran-canaria-plan`) that holds financial/PII content. **Never record `/init` against the real repo** (or `~/repos/gran-canaria-plan`, or any `/tmp/gcp-ep17*` clone) — record only against the committed synthetic fixture at `../demo-assets/ep17-standin/`. The copy below is clean (no figures, names, or addresses); the risk is only in the screen recording.

---

## LinkedIn (upload the 4:5 natively — no link in the body)

This project has zero lines of code. Claude Code still wrote its own onboarding file in one command.

I pointed it at my family's move-abroad plan — a static site plus a folder of markdown notes, no CLAUDE.md anywhere. Then I typed `/init`.

It scanned the whole project, worked out the shape on its own, and wrote a clean CLAUDE.md to the root: a short summary and a map of how things are organized. Claude writes that file itself — you never touch it.

That file auto-loads at the start of every future session, so it already knows the layout before I ask for anything.

Run the Docs, the Claude Code series. A project by Invotek.

#ClaudeCode #AICoding #DeveloperTools

**First comment:**
> Watch the 60-second demo: [YouTube link]

---

## TikTok / Instagram Reels

claude code writes its own onboarding file in one command. zero code in this one — i ran /init on my move-abroad plan (just a static site + some markdown) and it scanned the project and generated a clean CLAUDE.md map that auto-loads every session after. follow for a Claude Code tip a day.

#claudecode #aitools #coding #devtools

---

## YouTube Shorts

**Title:** Claude Code /init: let Claude write its own CLAUDE.md

**Description:**
This project has zero lines of code — and Claude Code still wrote its own onboarding file in one command.

I ran `/init` on my family's move-abroad plan: a static site plus a folder of markdown notes, no CLAUDE.md anywhere. Claude analyzed the project, worked out the structure on its own, and wrote a clean CLAUDE.md to the root — a short summary and a map of how things are organized. You never write that file; Claude does.

That CLAUDE.md auto-loads at the start of every future session, so Claude already knows the layout before you ask for anything.

Run the Docs, the Claude Code series. A project by Invotek.

#Shorts #ClaudeCode #AICoding

---

## X / Twitter

This project has zero lines of code. Claude Code still wrote its own onboarding file in one command.

I ran `/init` on a plan that's almost all docs — a static site plus markdown. It scanned everything and wrote its own CLAUDE.md: a map of the structure that auto-loads at the start of every future session.
