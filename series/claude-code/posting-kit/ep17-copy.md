# Ep 17 — Let Claude write its own onboarding (`/init` → CLAUDE.md)

**Suggested slot:** weekday 09:00 CET
**Files:**
- `ep17-linkedin-4x5.mp4` (1080×1350 — LinkedIn + X)
- `ep17-vertical-9x16.mp4` (1080×1920 — TikTok / YouTube Shorts / Reels)

> ⚠️ **PRODUCTION SAFETY (read `../production/ep17.NOTES.md` before recording).** The story references a *real* private repo (`gran-canaria-plan`), but that repo holds financial/PII content. **Do NOT record `/init` against the real repo** — record against the sanitized stand-in described in the notes. The copy below is clean (no figures, names, or addresses); the risk is only in the screen recording.

---

## LinkedIn (upload the 4:5 natively — no link in the body)

Your project has zero lines of code. Claude Code still writes its own onboarding file in one command.

I pointed it at my family's move-abroad plan — a static site plus a folder of markdown notes, no CLAUDE.md anywhere. Then I typed `/init`.

It scanned the whole project, worked out the shape on its own, and wrote a clean CLAUDE.md to the root: a short summary and a map of how things are organized. Claude writes that file itself — you never touch it.

That file auto-loads at the start of every future session, so it already knows the layout before I ask for anything.

**First comment:**
> Watch it: [YouTube link]

#ClaudeCode #AICoding #DeveloperTools

---

## TikTok / Instagram Reels

claude code writes its own onboarding file in one command. zero code needed — i ran /init on my move-abroad plan (just a static site + some markdown) and it generated a clean CLAUDE.md map that auto-loads every session after. follow for a Claude Code tip a day.

#claudecode #aitools #coding #devtools

---

## YouTube Shorts

**Title:** Claude Code /init: let Claude write its own CLAUDE.md

**Description:**
`/init` generates a starting CLAUDE.md automatically — Claude analyzes your project and writes a file mapping the structure and conventions it discovers, then auto-loads it at the start of every future session.

In this one I point Claude Code at a plan that's almost all docs — a static HTML site plus a folder of markdown notes, with no CLAUDE.md yet. I run `/init`, it scans the whole thing, and writes a clean onboarding file to the root. Every session after that starts already knowing the layout.

Run the Docs, Claude Code series. A project by Invotek.

#Shorts #ClaudeCode #AICoding

---

## X / Twitter

Your project has zero lines of code. Claude Code still writes its own onboarding file in one command.

I ran `/init` on a plan that's almost all docs — a static site plus markdown. It scanned everything and wrote its own CLAUDE.md: a map of the structure that auto-loads at the start of every future session.
